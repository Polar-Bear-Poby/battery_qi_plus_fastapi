# ------------------------
# 💡 Default ignored files
# ------------------------
/shelf/
/workspace.xml

# ------------------------
# 💡 Editor-based HTTP Client requests
# ------------------------
/httpRequests/

# ------------------------
# 💡 Datasource local storage ignored files
# ------------------------
/dataSources/
/dataSources.local.xml

# ------------------------
# ✅ 1. 환경 변수 파일 (.env) - DB 정보, API 키 등 보안 정보 포함
# ------------------------
config/.env

# ------------------------
# ✅ 2. 가상 환경 관련 파일 (Poetry, Conda, Virtualenv)
# ------------------------
.venv/
conda-meta/
env/
venv/
pip-selfcheck.json

# ------------------------
# ✅ 3. Python 캐시 파일 및 컴파일된 파일 (.pyc, .pyo)
# ------------------------
__pycache__/
*.py[cod]

# ------------------------
# ✅ 4. 로그 파일 및 실행 중 생성되는 파일
# ------------------------
logs/
*.log

# ------------------------
# ✅ 5. IDE 설정 파일 (PyCharm, VSCode 등)
# ------------------------
.idea/
.vscode/

# ------------------------
# ✅ 6. OS별 불필요한 파일 (Windows, macOS)
# ------------------------
.DS_Store
Thumbs.db

# ------------------------
# ✅ 7. Docker 관련 (컨테이너 내부에서 자동 생성되는 파일)
# ------------------------
Dockerfile
*.dockerignore
docker-compose.yml

# ------------------------
# ✅ 8. 대형 모델 가중치 파일 (최대 용량 제한 고려)
# ------------------------
app/models/*.pt
app/models/*.pth

# ------------------------
# ✅ 9. 테스트 및 빌드 관련 폴더
# ------------------------
tests/__pycache__/
dist/
build/

# ------------------------
# ✅ 10. MySQL, SQLite 등 DB 관련 임시 파일
# ------------------------
*.sqlite
*.db

# ------------------------
# ✅ 11. Jupyter Notebook 체크포인트 (사용 시)
# ------------------------
.ipynb_checkpoints/

# ------------------------
# ✅ 12. Poetry 관련 파일 (GitHub 원격에는 `poetry.lock` 제외)
# ------------------------
poetry.lock

